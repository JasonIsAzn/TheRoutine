import { View, Text } from 'react-native';

export default function WorkooutPlanScreen() {
    return (
        <View className="flex-1 justify-center items-center bg-white">
            <Text className="text-xl">🏋️ gains</Text>
        </View>
    );
}
