import { View, Text } from 'react-native';

export default function DietTrackerScreen() {
    return (
        <View className="flex-1 justify-center items-center bg-white">
            <Text className="text-xl">🍽️ big back</Text>
        </View>
    );
}
